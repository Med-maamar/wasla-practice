/**
 * PostCSS configuration for Tailwind CSS v4.
 *
 * Next.js reads this file automatically during builds and local development.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
